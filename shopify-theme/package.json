{
  "name": "aunties-eats-shopify-theme",
  "private": true,
  "version": "1.0.0",
  "description": "Shopify theme extracted from the Aunties project",
  "scripts": {
    "dev": "shopify theme dev --path ..",
    "push": "shopify theme push --path ..",
    "login": "shopify login --store $SHOPIFY_STORE"
  }
}
